<%- include header %>

<% posts.forEach(function(post,index){  %>
	<p>
		<h2><a href="/u/<%= user.name%>/<%= post.time.day %>/<%= post.title %>"><%= post.title %></a></h2>
	</p>
	<p class="info">
		作者:<a href="/u/<%=user.name %>"><%= post.name %></a> |
		日期:<%= post.time.day %>
	</p>
	<p><%- post.post %></p>
<%	}); %>

	this is index page!
<%- include footer %>
